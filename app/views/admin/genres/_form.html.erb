<%= form_with model: genre, url: url do |f| %>
  <%= f.text_field :name, placeholder: "ジャンル名入力" %>

  <% if current_page?(admin_genres_path) %>
    <%= f.submit '新規登録', class: "btn btn-success" %>
  <% else %>
    <%= f.submit '更新', class: "btn btn-success" %>
  <% end %>

<% end %>